info:
  name: 9 - Jouer un Coup - Succès (Joueur Actif)
  type: http
  seq: 9

http:
  method: POST
  url: "{{gameServiceUrl}}/games/{{gameId}}/moves"
  headers:
    - name: X-UserId
      value: "{{currentPlayerId}}"
  body:
    type: json
    data: |-
      {
        "x": 1,
        "y": 1
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Coup joué avec succès (200 OK)", function() {
          expect(res.getStatus()).to.equal(200);
          expect(res.body.id).to.equal(bru.getVar("gameId"));
          // Le joueur courant doit avoir changé
          expect(res.body.currentPlayerId).to.not.equal(bru.getVar("currentPlayerId"));
        });
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

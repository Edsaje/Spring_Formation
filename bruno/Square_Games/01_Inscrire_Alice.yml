info:
  name: 1 - Inscrire Utilisateur (Alice - ROLE_USER)
  type: http
  seq: 1

http:
  method: POST
  url: "{{userServiceUrl}}/users"
  body:
    type: json
    data: |-
      {
        "username": "{{aliceUsername}}",
        "email": "{{aliceEmail}}",
        "password": "password123",
        "role": "ROLE_USER"
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const ts = Date.now();
        bru.setVar("aliceUsername", "alice_" + ts);
        bru.setVar("aliceEmail", "alice_" + ts + "@example.com");
    - type: after-response
      code: |-
        bru.setVar("aliceId", res.body.id);
    - type: tests
      code: |-
        test("Alice inscrite avec succès (201 Created)", function() {
          expect(res.getStatus()).to.equal(201);
          expect(res.body.id).to.be.a('string');
          expect(res.body.username).to.equal(bru.getVar("aliceUsername"));
          expect(res.body.role).to.equal("ROLE_USER");
        });
  assertions:
    - expression: res.status
      operator: eq
      value: "201"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
